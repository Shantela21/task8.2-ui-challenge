export function Button1({ name }) {
  return (
    <>
      <button className="button1 desktop">{name}</button>
    </>
  );
}

export function Button2({ name }) {
  return (
    <>
      <button className="button2">{name}</button>
    </>
  );
}
